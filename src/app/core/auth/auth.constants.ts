/** Chave do token JWT no localStorage (partilhada com o interceptor HTTP). */
export const AUTH_TOKEN_STORAGE_KEY = 'monitor_ambiental_jwt';

/** Chave do refresh token no localStorage. */
export const AUTH_REFRESH_TOKEN_STORAGE_KEY = 'monitor_ambiental_refresh';
